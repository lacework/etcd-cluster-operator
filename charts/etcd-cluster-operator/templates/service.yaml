apiVersion: v1
kind: Service
metadata:
  name: {{ include "etcd-cluster-operator.fullname" . }}
  labels:
    {{- include "etcd-cluster-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "etcd-cluster-operator.selectorLabels" . | nindent 4 }}
